from setuptools import setup, find_packages

setup(
    name="file_explorer",      # Имя пакета
    version="0.1",             # Версия пакета
    packages=find_packages(),  # Находит все пакеты в проекте
)
