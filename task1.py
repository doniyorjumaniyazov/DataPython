# Треугольник существует только тогда, когда сумма любых двух его сторон больше третьей. 
# Дано a, b, c - стороны предполагаемого треугольника. Требуется сравнить длину каждого отрезка-стороны 
# с суммой двух других. Если хотя бы в одном случае отрезок окажется больше суммы двух других, то треугольника
# с такими сторонами не существует. Отдельно сообщить является ли треугольник разносторонним, 
# равнобедренным или равносторонним.


a = float(input("Введите длину стороны a: "))
b = float(input("Введите длину стороны b: "))
c = float(input("Введите длину стороны c: "))


if a + b > c and a + c > b and b + c > a:
    print("Треугольник существует.")
    
    # Определение типа треугольника
    if a == b == c:
        print("Треугольник равносторонний.")
    elif a == b or b == c or a == c:
        print("Треугольник равнобедренный.")
    else:
        print("Треугольник разносторонний.")
else:
    print("Треугольник не существует.")
    
    
# Напишите код, который запрашивает число и сообщает является ли оно простым или составным. 
# Используйте правило для проверки: “Число является простым, если делится нацело только на единицу
# и на себя”. Сделайте ограничение на ввод отрицательных чисел и чисел больше 100 тысяч.


# Ввод числа
number = int(input("Введите число: "))

# Проверка на отрицательные числа и числа больше 100000
if number < 0 or number > 100000:
    print("Число должно быть положительным и не превышать 100000.")
else:
    # Проверка на простоту числа
    if number <= 1:
        print(f"Число {number} не является простым.")
    elif number == 2:
        print(f"Число {number} является простым.")
    else:
        # Проверка делимости от 2 до числа-1
        is_prime = True
        for i in range(2, number):
            if number % i == 0:
                is_prime = False
                break
        
        if is_prime:
            print(f"Число {number} является простым.")
        else:
            print(f"Число {number} является составным.")


# Программа загадывает число от 0 до 1000. Необходимо угадать число за 10 попыток. 
# Программа должна подсказывать “больше” или “меньше” после каждой попытки. Для генерации случайного 
# числа используйте код:from random import randintnum = randint(LOWER_LIMIT, UPPER_LIMIT)

from random import randint

# Устанавливаем нижний и верхний пределы для случайного числа
LOWER_LIMIT = 0
UPPER_LIMIT = 1000

# Загадать случайное число
num = randint(LOWER_LIMIT, UPPER_LIMIT)

# Количество попыток
attempts = 10

# Приветствие
print("Я загадал число от 0 до 1000. Попробуйте угадать его за 10 попыток.")

# Цикл для 10 попыток
for attempt in range(1, attempts + 1):
    # Запрос числа у пользователя
    guess = int(input(f"Попытка {attempt}. Введите ваше число: "))
    
    # Проверка, угадал ли пользователь
    if guess < num:
        print("Мое число больше.")
    elif guess > num:
        print("Мое число меньше.")
    else:
        print(f"Поздравляю! Вы угадали число {num} за {attempt} попыток!")
        break
else:
    print(f"Вы не угадали число за {attempts} попыток. Загаданное число было {num}.")


